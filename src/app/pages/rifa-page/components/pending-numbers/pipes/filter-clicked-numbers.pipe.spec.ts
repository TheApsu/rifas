import { FilterClickedNumbersPipe } from './filter-clicked-numbers.pipe';

describe('FilterClickedNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterClickedNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
